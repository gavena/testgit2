this is my first git file, dont mess with it!
#to stage a file
git add <filename>
#to commit a file
git commit -m "message"
#to list status
git status

#to add remote orgin
git remote add origin https://github.com/gavena/testgit2.git

#to push committed files from local to remote
git push -u origin <branchname>
